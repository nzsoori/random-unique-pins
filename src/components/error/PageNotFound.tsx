import React from "react";
import { Link } from "react-router-dom";

const ErrorPage = () => (
  <div>
    <h1>
      Nothing to see here, go back to <Link to="/">Home</Link>
    </h1>
  </div>
);

export default ErrorPage;
